---
description: 'A ShellApp app.'
labels: ['app', 'react']
---

A React Module federation app for ShellApp. 

## Run the Module federation app

Import the app and add the following config to your workspace.jsonc:

```
bit use shell-app 
```

To run the app locally, run the following:

```
bit run shell-app 
```